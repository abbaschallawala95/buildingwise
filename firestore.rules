/**
 * This ruleset is configured for open access during development.
 * It allows any client to perform read, write, and delete operations on the
 * database without requiring authentication.
 *
 * WARNING: This is insecure and should NOT be used in a production environment.
 * Before deploying to production, you must implement proper security rules
 * that enforce authentication and authorization.
 */
rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {

    /**
     * @description Allow all operations on any document in any collection.
     * @path /{document=**}
     * @allow (read, write) Anyone, for development purposes.
     */
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
